**7. Explain Cookie**

A cookie is a small piece of data that a server asks a browser to store, and the browser automatically sends it back to the server on subsequent requests to the same site.

Think of it as the server’s way to remember state in a stateless HTTP world.

